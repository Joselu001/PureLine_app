//package com.cgi.loginapp; // Asegúrate de colocar el paquete adecuado

//import org.springframework.context.annotation.Bean;
//import org.springframework.context.annotation.Configuration;
//import org.springframework.web.servlet.config.annotation.CorsRegistry;
//import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

//@Configuration
//public class WebConfig implements WebMvcConfigurer {
    //@Override
    //public void addCorsMappings(CorsRegistry registry) {
        // Permitir solicitudes desde localhost:4200 (tu frontend Angular)
        //registry.addMapping("/**")
        //    .allowedOrigins("http://localhost:4200") // Permite solicitudes desde el frontend
      //      .allowedMethods("GET", "POST", "PUT", "DELETE") // Permite estos métodos
    //        .allowedHeaders("*"); // Permite todos los encabezados
  //  }
//}
